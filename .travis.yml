language: node_js
node_js:
  - "8"

sudo: false

before_install:
  - rvm install 2.6
  - rvm use 2.6
  - gem install hiptest-publisher
  - "hiptest-publisher -c cucumber-js.conf --only=features,step_definitions --test-run-id=258520"
  - npm install

script:
  - npm run ci > report.json

after_script:
  - hiptest-publisher -c cucumber-js.conf --push=results.json --test-run-id=258520


